// Production environment - replaced at build time via angular.json's
// fileReplacements when building with `ng build --configuration production`.
export const environment = {
  production: true,
  apiBaseUrl: 'http://localhost:8000',
};
